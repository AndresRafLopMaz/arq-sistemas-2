services:
  web1:
    image: nginx:alpine
    volumes:
      - ./server1/index.html:/usr/share/nginx/html/index.html:ro

  web2:
    image: nginx:alpine
    volumes:
      - ./server2/index.html:/usr/share/nginx/html/index.html:ro

  lb:
    image: nginx:alpine
    volumes:
      - ./lb/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:80"
    depends_on:
      - web1
      - web2
